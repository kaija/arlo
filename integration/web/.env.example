# Provider credentials — fill in at least one
OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://custom-endpoint.com/v1   # optional: OpenAI-compatible endpoint
# ANTHROPIC_API_KEY=sk-ant-...

# Model to use, format: provider:model
ARLO_MODEL=openai:gpt-4o

# ARLO_URL is set by compose.yaml (http://arlo:8080) — do NOT add it here
