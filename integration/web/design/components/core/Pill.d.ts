/** Mono-font metadata tag (tech stack, protocol names). Proper nouns stay untranslated. */
export interface PillProps {
  children?: React.ReactNode;
}
export declare function Pill(props: PillProps): JSX.Element;
