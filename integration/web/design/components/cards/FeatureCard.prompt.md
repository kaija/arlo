Card for the "principles" grid — icon box, title, short body.

```jsx
<FeatureCard icon={<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">…</svg>} title="Bring your own keys">
  Your credentials stay on your machine and go straight to your provider.
</FeatureCard>
```

Icons are 24px 2px-stroke line icons (Feather-style) drawn with currentColor. Grid: 3 columns, 24px gap, on the secondary background.
