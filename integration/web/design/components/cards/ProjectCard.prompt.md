Tile for one Arlo project — mark, name, description, tech pills, links.

```jsx
<ProjectCard project="rust" title="Arlo Rust" pills={['Rust','TUI · CLI','MCP']} siteHref="https://rust.arlo-ai.app" repoHref="https://github.com/kaija/arlo">
  A private, local-first agent that speaks whichever interface you need.
</ProjectCard>
```

Three-up grid, 24px gap. Links row pins to the bottom (flex column + margin-top auto).
