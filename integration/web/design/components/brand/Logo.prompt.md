Brand mark for the Arlo AI project family — a glyph in a #5856D6 rounded square; one variant per project.

```jsx
<Logo project="arlo" size={28} withWordmark />
<Logo project="rust" size={40} />
```

Projects: `arlo` (hub "A"), `rust` (">" prompt), `lite` (peak with bar), `agui` (waveform). Sizes in use: 40 project-card, 28 nav, 24 footer. Never redraw the glyphs.
