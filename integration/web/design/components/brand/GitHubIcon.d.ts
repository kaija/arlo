/** The GitHub octocat glyph used in nav and hero buttons (fill: currentColor). */
export interface GitHubIconProps {
  /** px. 16 in nav button, 20 in hero secondary button. @default 16 */
  size?: number;
}
export declare function GitHubIcon(props: GitHubIconProps): JSX.Element;
