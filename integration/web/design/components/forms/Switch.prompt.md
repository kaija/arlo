Toggle — for settings that take effect immediately.

```jsx
<Switch label="Local-only mode" defaultChecked />
<Switch label="Verbose logs" size="sm" />
```

- Knob travel is 18px on a 250ms ease — matches every other brand transition.
- No "on/off" text next to the track; the label alone carries the meaning.
